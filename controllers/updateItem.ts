import express = require("express");
import Item = require("../classes/Item");

function updateItem(updateData, id, req: express.Request, res: express.Response) {
   this.updateItem(function(err, items: Item[]) {

   });
};

export = updateItem;
